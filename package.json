{
  "name": "runsql",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "concurrently -k -n frontend,backend -c white,gray \"npm --prefix frontend run dev\" \"node scripts/start-backend.mjs\"",
    "build": "npm --prefix frontend run build",
    "test:backend": "node scripts/test-backend.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
