{
  "name": "runsql",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c white,gray \"npm --prefix frontend run dev\" \"backend/.venv/bin/python -m uvicorn backend.app.main:app --reload --host 127.0.0.1\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
